<===> 04_variable_double/input.scss
$input: "dquoted";
.result {
  output: #{#{$input}};
  output: #{"[#{$input}]"};
  output: #{"#{$input}"};
  output: #{'#{$input}'};
  output: #{"['#{$input}']"};
}

<===> 04_variable_double/output.css
.result {
  output: dquoted;
  output: [dquoted];
  output: dquoted;
  output: dquoted;
  output: ['dquoted'];
}
