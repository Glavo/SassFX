<===> issue_510/input.scss
@use "sass:map";
$before: map.remove((foo: 1, bar: 2, baz: 3, burp: 4), bar, baz);
$after: (foo: 1, burp: 4);

div {
  foo: $before == $after;
}
<===> issue_510/output.css
div {
  foo: true;
}
