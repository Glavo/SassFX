<===> issue_87/input.scss
$bar: "bar";
$foobar: "foo#{$bar}";
#{$bar} {
  #{$bar}: #{$bar};
  #{$bar}: $bar;
}
#{$foobar} {
  #{$foobar}: #{$foobar};
  #{$foobar}: $foobar;
}
<===> issue_87/output.css
bar {
  bar: bar;
  bar: "bar";
}

foobar {
  foobar: foobar;
  foobar: "foobar";
}
