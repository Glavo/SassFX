<===> issue_1263/input.scss
foo {
  @ap#{pl}y;
  @apply(--bar);
  @apply  (  --bar  );
  @ap#{pl}y   (   --bar , --foo  )  ;
}
<===> issue_1263/output.css
foo {
  @apply;
  @apply (--bar);
  @apply ( --bar );
  @apply ( --bar , --foo );
}
